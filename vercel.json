{
  "buildCommand": "hugo --minify",
  "outputDirectory": "public",
  "installCommand": "curl -L https://go.dev/dl/go1.22.0.linux-amd64.tar.gz | tar -C /usr/local -xz && export PATH=$PATH:/usr/local/go/bin && hugo mod download"
}
